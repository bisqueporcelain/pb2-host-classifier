# === GISAID data — NOT redistributable, do not commit ===
*.fasta
*.fa
*.fasta.gz
data/metadata.csv
results/**/metadata.csv

# Embeddings and attention tensors (large, derived from GISAID sequences)
*.npz
*.npy
*.pt
*.pth
*.ckpt

# Trained classifier weights. Derived from GISAID embeddings, so they are
# distributed only through the gated Hugging Face repo, where users must
# accept the GISAID Terms of Use first:
#   https://huggingface.co/bisqueporcelain/pb2-host-classifier
# Committing them here would bypass that gate.
*.joblib

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.Python

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# Model caches
.cache/
torch/hub/checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
